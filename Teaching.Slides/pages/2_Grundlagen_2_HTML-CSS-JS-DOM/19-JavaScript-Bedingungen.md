---
transition: slide-left
---

# JavaScript - Bedingungen

```js {monaco} { lineNumbers: 'on', editorOptions: { fontSize: 16, wordWrap: 'on' } }
if (condition) {
  // block of code to be executed if the condition is true
}
else {
  // block of code to be executed if the condition is false
}

switch (expression) {
  case x:
    // code block
    break;
  case y:
    // code block
    break;
  default:
    // code block
}
```
